common_inputs:
  # host: '{{$.host.camera}}'
  method: POST
  path: /camera/updateRtspSettings
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: updateRtspSettings|camera起点
  input:
    host: '{{$.host.cameraA}}'
    json:
      IsRtsp: true
      host: '{{$.host.replay65}}'
      rtsp_camera_type: 4
      startTime: '*'
      endTime: '*'
      play_channel: 5
  expectation:
    response:
      code: 0