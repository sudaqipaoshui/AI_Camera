common_inputs:
  method: GET
  path: /control/reboot
  params: {}
  headers:
    Content-Type: application/json
tests:
- case: reboot起点
  input:
    host: '{{$.host.cameraA}}'
  expectation:
    response:
      code: 200
      message: 'successful'
      # Version: 5.1.2-product

- case: reboot终点
  input:
    host: '{{$.host.cameraB}}'
  expectation:
    response:
      code: 200
      message: 'successful'