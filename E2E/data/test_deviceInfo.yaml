common_inputs:
  host: '{{$.host.cameraA}}'
  method: GET
  path: /camera/deviceInfo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/deviceInfo
  input:
  expectation:
    response:
      code: 0