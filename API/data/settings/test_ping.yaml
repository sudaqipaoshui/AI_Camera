common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/ping
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/ping
  input:
  expectation:
    response:
      code: 0