common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/activateInfo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/activateInfo
  input:
  expectation:
    response:
      code: 0
      # data.activateCode: '{{$.activateCode.testing}}'
      # data.result.licence:  '{{$.licence}}'