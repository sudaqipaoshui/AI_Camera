common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/resetStatusByTesterId?testerId={id}
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /resetStatusByTesterId
  input:
  expectation:
    response:
      code: 0