common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/checkHost
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/checkHost
  input:
  expectation:
    response:
      code: 0