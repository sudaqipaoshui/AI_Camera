common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/hostInfo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/hostInfo
  input:
  expectation:
    response:
      code: 0