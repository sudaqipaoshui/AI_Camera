common_inputs:
  host: '{{$.host.camera}}'
  method: POST
  path: /camera/changeHost
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/changeHost
  input:
    json:
      host: https://
  expectation:
    response:
      code: 0