common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/activate
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/activate
  input:
    json:
      activateCode: '{{$.activateCode.testing}}'
  expectation:
    response:
      code: 0