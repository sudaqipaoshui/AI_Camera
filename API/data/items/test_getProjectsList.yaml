common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/getProjectsList
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/getProjectsList
  input:
  expectation:
    response:
      code: 0