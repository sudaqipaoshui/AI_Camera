common_inputs:
  host: '{{$.host.camera}}'
  method: POST
  path: /camera/changeNoTwo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/changeNoTwo
  input:
    json:
      id: 17
      noTwoCheck: 1
  expectation:
    response:
      code: 0