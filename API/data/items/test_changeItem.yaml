common_inputs:
  host: '{{$.host.camera}}'
  method: POST
  path: /camera/deviceInfo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/deviceInfo
  input:
    json:
      id: 17
      forceChangeItem: false    # 可选，是否强制切换  
  expectation:
    response:
      code: 0