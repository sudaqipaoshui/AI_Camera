common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/getCmaSysInfo
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /getCmaSysInfo
  input:
  expectation:
    response:
      code: 0
      data.cma_sensor: ai_camera_v2
      data.sys_version: 20250930-1603