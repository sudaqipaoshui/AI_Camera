common_inputs:
  host: '{{$.host.camera}}'
  method: POST
  path: /camera/zoomInImage
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/zoomInImage
  input:
  expectation:
    json:
      ratio: 150
      item_id: 17
    response:
      code: 0