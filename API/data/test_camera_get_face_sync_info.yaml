common_inputs:
  host: '{{$.host.camera}}'
  method: GET
  path: /camera/camera_get_face_sync_info
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/getFaceSyncInfo
  input:
  expectation:
    response:
      code: 0