common_inputs:
  host: '{{$.host.camera}}'
  method: POST
  path: /camera/raceConfig
  params: {}
  headers:
    Content-Type: application/json
    x_token: '{{$.users[?(@.id==1)].x_token}}'
tests:
- case: /camera/raceConfig
  input:
    json: 
      itemGroup: 1
  expectation:
    response:
      code: 0